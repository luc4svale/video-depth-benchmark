# Results

- `vda_metrics.csv`, `depthcrafter_metrics.csv`, `chronodepth_metrics.csv` — per-model, per-sequence metrics plus a `MEDIA` row
- `all_models_metrics_consolidated.csv` — all models combined, matches Table I (MEDIA rows) and Table II (per-sequence rows) in the paper
- `comparacao_metricas_tum.png` / `.pdf` — bar chart comparing average metrics across the three models

Regenerated by `notebooks/05_evaluate_and_plot.ipynb` (or `scripts/evaluate_all.py` + `scripts/plot_metrics.py`) from the raw predictions in `predictions/`.
